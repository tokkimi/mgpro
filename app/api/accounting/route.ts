import {NextResponse} from 'next/server';
import {identity,db} from '@/lib/supabase';
import {sameOrigin} from '@/lib/access';
import {z} from 'zod';
export async function POST(req:Request){if(!sameOrigin(req))return NextResponse.json({error:'Origine refusée.'},{status:403});const user=await identity();if(user?.role!=='admin')return NextResponse.json({error:'Accès refusé.'},{status:403});const p=z.object({action:z.enum(['issue','payment']),id:z.string().uuid(),amount:z.number().positive().optional(),date:z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional(),reference:z.string().max(200).optional()}).safeParse(await req.json());if(!p.success)return NextResponse.json({error:'Opération invalide.'},{status:400});const {data,error}=await db().rpc('invoice_operation',{p_id:p.data.id,p_actor:user.id,p_action:p.data.action,p_amount:p.data.amount||0,p_date:p.data.date||'',p_reference:p.data.reference||''});return error?NextResponse.json({error:error.message},{status:409}):NextResponse.json({ok:true,data});}
