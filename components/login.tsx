'use client';
import {useState} from 'react';
import Link from 'next/link';
import {ArrowLeft,ArrowRight,LockKeyhole} from 'lucide-react';
import {Logo} from './public-site';
export default function Login({ready}:{ready:boolean}){const [error,setError]=useState('');const [busy,setBusy]=useState(false);return <div className="login-page"><div className="login-intro"><Logo/><div><p className="eyebrow">VOTRE ESPACE MG PRO</p><h1>Votre projet.<br/><em>Toujours à portée<br/>de main.</em></h1><p>Retrouvez vos documents, échangez avec notre équipe et suivez l’avancement de vos travaux.</p></div><Link href="/"><ArrowLeft size={16} aria-hidden="true"/> Retour au site</Link></div><div className="login-form"><LockKeyhole size={29}/><h2>Heureux de vous retrouver.</h2><p>Connectez-vous à votre espace sécurisé.</p><form onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget);try{const res=await fetch('/api/auth',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password')})});const d=await res.json();if(!res.ok)throw new Error(d.error);location.href='/admin';}catch(e){setError((e as Error).message)}finally{setBusy(false)}}}><label>Courriel<input name="email" type="email" required autoComplete="email" placeholder="vous@exemple.com"/></label><label>Mot de passe<input name="password" type="password" required autoComplete="current-password"/></label>{error&&<p className="error" role="alert">{error}</p>}<button className="btn primary full" disabled={busy}>{busy?'Connexion…':'Se connecter'}<ArrowRight size={18}/></button></form>{!ready&&<div className="notice">L’activation des comptes sécurisés est en attente de la connexion à la base de données.</div>}<div className="demo-link"><p>Découvrir le nouvel espace de gestion</p><Link href="/demo">Ouvrir la démonstration <ArrowUpRightIcon/></Link><small>Données fictives · aucune donnée client réelle</small></div></div></div>}
function ArrowUpRightIcon(){return <ArrowRight size={15}/>}
