import test from 'node:test';import assert from 'node:assert/strict';import {accountingReport,journalCsv} from '../lib/accounting';import {RecordItem} from '../lib/model';
const row=(date:string,entries:any[])=>({id:crypto.randomUUID(),client_id:null,project_id:null,version:1,updated_at:date,kind:'journal',created_at:date,data:{date,title:'Journal',entries}} as RecordItem);
test('period report carries opening balances but excludes opening income from current result',()=>{const rows=[row('2025-12-31',[{account:'1000 Banque',debit:100,credit:0},{account:'4000 Revenus',debit:0,credit:100}]),row('2026-01-15',[{account:'1000 Banque',debit:20.1,credit:0},{account:'4000 Revenus',debit:0,credit:20.1}]),row('2026-02-02',[{account:'1000 Banque',debit:50,credit:0},{account:'4000 Revenus',debit:0,credit:50}])];const r=accountingReport(rows,'2026-01-01','2026-01-31');assert.equal(r.balance['1000 Banque'],120.1);assert.equal(r.revenue,20.1);assert.equal(r.journals.length,1)});
test('CSV neutralizes spreadsheet formula injection including leading whitespace',()=>{const r=row('2026-01-01',[{account:' =HYPERLINK("bad")',debit:0,credit:0}]);assert(journalCsv([r]).includes('"\' =HYPERLINK'))});
